# 인수인계

- 파일럿 배포 사전점검·상태 감지와 기본 비활성화된 `외부 상태 감시` 구현·정적 검증은 완료했지만 실제 공개 URL의 저장소 변수는 설정하지 않았다.
- 워크플로가 `main`에 반영된 뒤 README 순서대로 실제 URL의 수동 성공을 확인하고 예약 검사를 활성화한다.
- 첫 예약 실행과 담당 계정의 GitHub Actions 실패 알림 수신은 아직 검증하지 않았다.
- 실제 파일럿 데이터를 넣기 전 암호화 원격 저장소에서 덤프와 보조 파일을 내려받아 별도 환경으로 가져오고, `last-restore-recovery-targets.tsv`를 따른 팀별 새 키 발급·이전 링크 `403`·복구 완료 재백업까지 자동 품질 게이트 밖의 실제 자격 증명으로 확인해야 한다.
- 첫 그룹 스터디 실사용에서 조직 연속성 레이더가 놓칠 뻔한 책임이나 인수인계 공백을 한 번 이상 미리 발견하는지 확인하고 오탐과 행동 문구를 기록해야 한다.
- BRIEF 이벤트 v2와 `2.0.0-rc.1` 계약 팩을 고정하고 Java record 직렬화를 검증했으며, V22에 신호별 현재 상태·연속 리비전과 불변 outbox를 추가했다. 명시적 재조정 유스케이스는 동일 계산 무변경, `ACTIVE → RESOLVED → ACTIVE`, 심각도 변경과 호출자 원본 트랜잭션의 함께 롤백을 MySQL에서 검증했다. 기존 워크스페이스 변경과 시간 스케줄러는 아직 이 유스케이스를 자동 호출하지 않고 송신기·실제 BRIEF HTTP 전달도 없으므로, 다음 진입점은 원본 변경 경계와 재조정 트리거 연결이다.
- 첫 그룹 스터디에서 과거 결정의 결과·이유·관련 역할을 탐색 화면에서 짧은 흐름으로 다시 찾을 수 있는지 확인하고, 놓친 검색어·필터와 V14 이전 시각 미상 안내의 이해도를 기록해야 한다.
- 실제 공개 HTTPS 스테이징에서 WATCH가 보낸 최초 상태 변경 이벤트와 응답 유실 뒤 같은 `eventId` 재전송이 BATON 인박스 한 건으로 수렴하고 WATCH 전달 적체가 비는지 아직 검증하지 않았다.
- 외부 전송용 모니터 토큰과 이벤트 수신기 토큰을 서로 다르게 배포하고 양쪽 로그에 인증값이 남지 않는지 확인한 뒤에만 `BATON_WATCH_EVENT_RECEIVER_ENABLED`와 WATCH 콜백 전달을 운영에서 활성화한다.
- 실제 Google·Naver·SMTP 자격 증명과 공개 HTTPS 출처에서 세 로그인 흐름, 콜백 로그 비노출, 이메일 수신·검증과 세션 쿠키 속성을 확인한 뒤에만 계정 인증 게이트를 운영에서 활성화한다.
- 로컬에서는 `e2e:fullstack`이 실제 로컬 세션 → AccountMembership 연결 → 서버 권위 방 매핑 → 참여권 갱신과 쿠키·JWK 서명의 생산자 경계를, 교차 서비스 테스트가 BATON 서명자·JWK → ROUND TURN·WebSocket의 소비자 경계를 각각 고정한다. 별도 `e2e:round-edge`도 사설 CA의 테스트 전용 Caddy와 기존 ROUND 이미지를 연결해 같은 브라우저의 `Secure` 쿠키로 TURN 자격 증명을 발급받고 WSS 방에 입장하는 흐름을 고정했다. 프로덕션 Caddy·Compose에는 다이제스트 고정 ROUND 런타임, 정확한 참여 쿠키 프록시, 인증 전 요청률 제한과 복구·생명주기 상호 배제를 선택적으로 반영했다. 다음 과제는 실제 릴리스 다이제스트와 외부 coturn을 공개 HTTPS 스테이징에 배포해 UDP·TCP·TLS 할당·미디어 중계, 실제 OAuth 계정의 동일 `sub` 입장과 키 중첩 회전을 검증하는 것이다.
