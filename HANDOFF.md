# 인수인계

## 해소 상세와 생성 후 관련 화면 갱신

- 이번 주 해소 건수를 누르면 해소한 업무·시점을 펼치고 현재 업무로 이동한다. 원본 참조와
  해소 리비전은 접힌 ‘해소 근거 보기’에서 확인한다. BRIEF의 같은 조회에서 계산한 전체
  건수와 상세를 중계하며 마지막 해소 상태 수정 시각으로 해소 시점을 바꾸지 않는다.
- 해소 목록의 배타 커서와 1~100건 조회를 기존 서비스 경로로 전달한다. 다음 페이지의
  주차·시간대가 이전 페이지와 다르면 목록을 감추고 첫 페이지 재조회를 안내한다. 목록
  누락과 잘못된 외부 상세 응답은 계약 오류로 처리한다. BRIEF 상세 응답을 먼저 적용해야 한다.
- 새 생성·기존 브리프 재사용에 성공하면 최신 브리프·이력과 현재 관심 요약·목록·해소 결과를
  다시 읽는다. 상태·심각도·공백 필터는 유지하고 목록·해소 커서와 선택한 상태 변화는
  초기화한다. 생성 실패에는 이 초기화를 적용하지 않는다.
- 프런트 빌드와 브라우저 30개 시나리오(Chromium·390px 모바일·WebKit)를 확인했다. 새 상세
  시나리오는 실제 업무 화면 전환·모바일 상세 창 복귀·브라우저별 날짜 표시에 맞춰 검증했다.
  기존 WebKit 두 건은 개발 서버 초기 로딩 지연이 있어 빌드된 화면에서 다시 실행해 통과했다.
  나머지 검증은 개발 서버에서 수행했으며 모든 브라우저 시나리오는 API 대역을 사용한다.
  임시 3179 포트 설정은 정리했다.
- 최종 `build checkApiContract`가 통과했다. 애플리케이션 404건·웹 118건·REST Docs 150건·
  외부 어댑터 82건·영속성 어댑터 36건·부트스트랩 66건에 실패·건너뜀은 없다. 생성 계약은
  해소 목록·마지막 커서 null·시각 형식·양의 해소 리비전·조회 크기를 반영했다.
- 최신 두 실행 JAR·MySQL 8.4·PostgreSQL 18.6·서비스 Caddy의
  `BriefEditionHttpsEndToEndTest` 1건이 통과했다. 해소 시각·리비전 중계와 특수 문자가 있는
  커서 전달, 빈 끝 페이지에서도 전체 건수가 유지되는지 확인했다. 기존 인증·권한·에디션
  시나리오도 같은 실행에서 검증했다. 조회 증거는 격리한 BRIEF DB에 준비했으며 실제 원본
  생산자 연동·공인 스테이징 검증의 범위를 늘린 것은 아니다.



## 이번 주 해소 요약과 지난주 바로 비교

- 해소 요약은 시즌 시간대의 이번 주에 연속된 활성→해소 증거가 있고 현재도 해소된 항목 수다.
  반복 해소 갱신은 해소 시점을 옮기지 않는다. 재활성화·해소 시점을 확정할 수 없는 공백은
  제외하며 활성 요약과 별도로 주차·확인 시각을 표시한다. 상세 기준은 PRD-0009와 BRIEF PRD-0030이다.
- ‘지난주와 바로 비교’는 선택한 불변 브리프의 저장 주차·시간대로 정확한 지난주 마지막
  에디션을 찾고 기존 비교에 연결한다. 화면에 로드된 이력에 없어도 찾으며 없음·장애·권한
  거부를 구분한다. 현재 시즌 시간대로 과거 비교를 다시 계산하지 않는다. PRD-0010을 따른다.
- 새 테이블·이벤트·스케줄러를 추가하지 않았다. BRIEF의 주간 해소 API와 서비스 Caddy 내부
  허용 경로를 먼저 적용한 뒤 이 BATON 화면을 반영해야 한다. 공개 Caddy에는 추가하지 않는다.
- `build checkApiContract`가 통과했다. 애플리케이션 404건·웹 118건·REST Docs 150건·외부
  어댑터 82건·영속성 어댑터 36건·부트스트랩 66건에 실패·건너뜀은 없다.
- 프런트 빌드와 API 대역 브라우저 27건(Chromium·390px 모바일·WebKit)이 통과했다.
  해소 0건·장애·권한 거부와 지난주 비교의 정상·없음·오류·권한 거부, 기존 탐색·생성·원본
  이동을 확인했다. 임시 3179 포트 설정은 정리했다.
- 최신 두 JAR·MySQL 8.4·PostgreSQL 18.6·서비스 Caddy의 `BriefEditionHttpsEndToEndTest`가
  성공했다. 해소 건수 중계·미인증·멤버십 거부, 지난주 없음·다른 팀 거부·정확한 시간대의
  최대 생성 순번과 서로 다른 규칙 버전 조회를 확인했다. 조회 증거는 격리한 BRIEF DB에
  준비했으며 실제 이벤트 생산·송신 검증을 추가로 수행한 것은 아니다.
- BRIEF 전체 `test :bootstrap:bootJar`와 이미지·Compose·신뢰한 로컬 HTTPS 조립 검증도
  통과했다. 공인 HTTPS 배포·자동 주간 생성·RELAY 전달은 이번 작업에 포함하지 않는다.


## BRIEF 탐색과 저장 이후 변경 안내

- [PRD-0010](docs/PRD/0010_brief-navigation-and-readiness/spec.md)의 이력·비교·현재 업무 연결에
  에디션별 추가 전달 확인과 상황별 행동 안내를 추가했다. 같은 에디션 재사용도 확인 기준을
  갱신하며 생성 근거 없음은 UNKNOWN으로 구분한다. 원본 참조·리비전은 상세 기록에 둔다.
- 기존 BATON 생성 실행·outbox를 읽으며 BRIEF 불변 본문·ETag·이벤트 계약과 양쪽 스키마를
  유지한다. 사용자 API 하나를 추가했고 실제 공개 Caddy 허용 목록은 바꾸지 않았다.
- 변경한 애플리케이션·MySQL 대상 테스트와 REST Docs/API 생성, 프런트 빌드가 통과했다.
  Chromium·390px 모바일·WebKit의 BRIEF 브라우저 24건도 성공했다. 기본 포트가 다른 작업에
  사용 중이어서 임시 3179 포트로 검증했으며 브라우저는 API 대역을 사용했다.
- 최종 `build checkApiContract`와 실제 BATON·BRIEF JAR, MySQL 8.4·PostgreSQL 18.6,
  서비스 Caddy의 `BriefEditionHttpsEndToEndTest`가 성공했다. 애플리케이션 기본 테스트
  402건·웹 118건·REST Docs 148건·외부 어댑터 81건·영속성 어댑터 36건·부트스트랩
  66건에 실패·건너뜀은 없었다.
- HTTPS 검증은 선택한 에디션의 범위 거부·미확정 생성 근거와 추가 전달·같은 에디션 재사용
  뒤 상태 갱신을 확인했다. 이 시나리오의 BRIEF 투영과 BATON 전달 완료 기록은 격리 DB에
  준비했으며 실제 이벤트 송신·공인 스테이징 검증을 뜻하지 않는다. BRIEF 제품은 바꾸지
  않아 앞서 검증한 실행 JAR을 사용했고 별도 BRIEF 전체 테스트는 반복하지 않았다.

## BRIEF 관심 항목 연결의 남은 작업

- 공개 이벤트 전달 origin은 `https://brief.b4ton.com`으로 정했다. 서버는 미구축 상태이며
  설정 예시만 반영했다. 내부 조회·생성 대상은 기존 `brief-service:8443`을 유지한다. 같은
  호스트에 배치할 때는 두 공개 Caddy의 기본 80·443 포트가 충돌하므로 앞단 통합이 먼저다.
  DNS·공인 인증서·배포 완료를 뜻하지 않으며 서버 위치·접속 방법·IP가 필요하다.
- 요약·필터·상태 전이와 최신 에디션 조회·생성 화면에 주간 분류와 원본 심각도를 추가했다.
  `codex/brief-attention-view`에서 분리한 `codex/brief-edition-carryover-ui` 브랜치이며
  BRIEF 선정 규칙 v2와 함께 배포해야 한다. 소비자 변경을 먼저 반영하고 실제 스테이징의
  서비스 인증서·비밀·비공개 HTTPS를 준비한 뒤 로그인 사용자 화면에서 확인해야 한다.
- 이번 주 변경과 이전부터 미해소는 저장된 `section`으로 구분한다. 이전 값 `null`은
  분류 미기록으로 표시하며 기존 성공 생성 실행을 강제로 재생성하지 않는다. 전이의 원본
  `sourceSeverity`는 긴급·주의로, v1 `null`은 미기록으로 표시한다.
- 앞선 주간 분류·원본 심각도 변경에서는 백엔드 `build checkApiContract`, 실제 두 JAR·MySQL 8.4·PostgreSQL
  18.6·서비스 Caddy의 `BriefEditionHttpsEndToEndTest`, 프런트 빌드와 BRIEF 브라우저
  12건이 통과했다. 현재 탐색 기능까지 포함한 검증은 위 항목을 따른다. 교차 테스트의 새 조회 데이터는 격리한 BRIEF DB로 준비했다. 전체 스모크와
  이벤트 생산·전달 테스트는 이번 변경에서 재실행하지 않았다. 화면 캡처 3건은 포트 충돌을
  피한 임시 3117 포트에서 확인했다. API 대역 브라우저와 로컬 HTTPS 결과를 실제 배포 완료로
  해석하지 않는다.
- 이벤트 계약 핀과 실제 record 직렬화 입력은 `2.0.0-rc.4`를 유지했다. 공인 HTTPS
  스테이징 검증 전에는 안정 버전으로 승격하지 않는다. BRIEF의 선택적 loopback 지표와
  BATON의 기존 전달 진단 명령을 실제 수집·경보 시스템에 연결하는 작업도 남아 있다.

## CAL 시즌 이름 연결의 남은 작업

- 전체 일정·필요한 취소·시즌 이름의 재전달 대상을 나타내는 매니페스트와 완료 신호를 정해야 한다.
  이름의 `REPLAY` 준비 완료나 대기 행 수만으로 CAL 복구 모드를 해제해서는 안 된다.
- 정식 채택 전 CAL 후보를 불변 사전 릴리스로 게시하고 검증된 자산으로 계약 핀을 갱신해야 한다.
  현재 안정 핀은 `1.0.0`이며 `BATON_CAL_SEASON_METADATA_ENABLED=false`와 보정 모드 `OFF`를
  유지한다. CAL V7 배포 확인 뒤 PRD-0006의 보정·복원 순서를 운영 환경에서 검증해야 한다.
  실제 캘린더 앱의 이름 갱신도 남아 있다.

## 다음 실행 순서

1. 실제 공개 URL을 `BATON_HEALTH_URL`에 설정하고 `외부 상태 감시` 워크플로의 수동 실행을 먼저 통과시킨다. 이후 `BATON_EXTERNAL_MONITOR_ENABLED=true`로 예약 검사를 켜고 첫 예약 실행과 담당 계정의 GitHub Actions 실패 알림 수신을 확인한다.
2. 실제 파일럿 데이터를 넣기 전에 암호화 원격 저장소의 덤프와 보조 파일을 별도 환경에 복구한다. `last-restore-recovery-targets.tsv`에 따라 팀별 새 키 발급, 이전 링크의 `403` 응답과 복구 완료 뒤 재백업까지 실제 자격 증명으로 확인한다.
3. CAL 전용 Bearer를 소유자 전용 파일에 저장하고 `BATON_CAL_BEARER_TOKEN_FILE`에 절대 경로를 설정해 프로덕션 사전점검을 통과시킨다. 기존 문자열의 NFC·제어 문자 적합성을 점검한 뒤 PRD-0006 순서대로 CAL 캡처와 보정을 먼저 활성화한다. `./ops/check-integration-delivery.sh`, `./ops/show-integration-metrics.sh`와 DB 상태가 정상이면 전달을 켜고 실제 시즌 피드를 확인한다.
4. BRIEF 전용 Bearer 파일과 HTTPS origin을 준비하고 전달을 끈 상태에서 재조정 주기를 설정해 초기 신호와 아웃박스를 확인한다. 이후 전달을 켜 `./ops/check-integration-delivery.sh`와 `integration="brief"` 공통 지표가 정상인지 확인하고, 재시도·동일 이벤트 재전달·심각도 변경·해소가 실제 BRIEF 관심 항목에 수렴하는지 확인한 뒤 새 token과 직전 token의 중첩 교체를 검증한다.
5. 외부 전송용 WATCH 모니터 토큰과 이벤트 수신기 토큰을 서로 다르게 배포하고 로그에 인증값이 남지 않는지 확인한다. 실제 공개 HTTPS에서 최초 상태 변경 이벤트와 응답 유실 뒤 같은 `eventId` 재전송이 BATON 인박스 한 건으로 수렴하고 WATCH 전달 적체가 비는지 확인한 뒤에만 운영 수신을 활성화한다.
6. 실제 Google·Naver·SMTP 자격 증명과 공개 HTTPS 출처에서 세 로그인 흐름, 콜백 로그 비노출, 이메일 수신·검증과 세션 쿠키 속성을 확인한다. `integration="email"`의 조치 대상 실패가 `0`이고 `./ops/check-integration-delivery.sh`가 성공하는지 확인한 뒤 계정 인증 게이트를 활성화한다.
7. 실제 릴리스 다이제스트와 외부 coturn을 공개 HTTPS 스테이징에 배포해 UDP·TCP·TLS 할당과 미디어 중계, 실제 OAuth 계정의 동일 `sub` 입장과 키 중첩 회전을 확인한다.
8. 5번의 WATCH 공개 스테이징 검증이 끝난 뒤에만 이벤트 순서·조정 정책을 확정하고 WATCH 상태 프로젝션과 UI를 구현한다.

## 파일럿 관찰

- 첫 그룹 스터디에서 조직 연속성 레이더가 놓칠 뻔한 책임이나 인수인계 공백을 한 번 이상 미리 발견하는지 확인하고 오탐과 행동 문구를 기록한다.
- 과거 결정의 결과·이유·관련 역할을 탐색 화면에서 짧은 흐름으로 다시 찾을 수 있는지 확인하고, 놓친 검색어·필터와 V14 이전 시각 미상 안내의 이해도를 기록한다.
