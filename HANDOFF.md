# 인수인계

## BRIEF 연결의 남은 작업

- 공개 이벤트 전달 주소는 `https://brief.b4ton.com`이다. 서버는 미구축 상태이며 서버 위치·접속
  방법·IP와 공인 HTTPS 준비가 필요하다. 같은 호스트에서는 Caddy의 80·443 포트를 먼저 통합한다.
- BRIEF 선정 규칙 v2와 주간 해결 상세 API를 먼저 배포한 뒤 BATON을 적용한다. 내부 조회·생성은
  `brief-service:8443`과 별도 서비스 인증을 사용하며 공개 Caddy에 조회 경로를 추가하지 않는다.
- 실제 로그인 사용자로 해결 상세·비교·생성, 특정 주간 요약 링크의 로그인 복귀와 인쇄·PDF 저장을
  확인해야 한다. 로컬 API 대역 검증을 공인 스테이징과 실기기 검증으로 확대하지 않는다.
- 공인 HTTPS 검증 전 계약 핀 `2.0.0-rc.4`를 안정 버전으로 올리지 않는다. 전달 진단과 BRIEF
  지표를 실제 수집·경보 시스템에 연결하는 작업도 남아 있다.
- 제품 동작은 [주간 업무 점검](docs/PRD/0009_brief-current-attention/spec.md)과
  [주간 요약 탐색·공유·출력](docs/PRD/0010_brief-navigation-and-readiness/spec.md)을 따른다.

## CAL 계약 채택의 남은 작업

- 전체 일정·필요한 취소·시즌 이름의 재전달 준비와 시즌별·전체 매니페스트 완료 신호를 구현했다.
  같은 복구 ID의 최신 아웃박스가 모두 전달된 뒤 CAL의 `COMPLETED`를 확인하며, 이름의 `REPLAY`
  준비 완료나 대기 행 수만으로 CAL 복구 모드를 해제하지 않는다.
- CAL 불변 사전 릴리스 `1.1.0-rc.1`의 GitHub 증명과 자산 SHA-256을 확인하고 일정·시즌 이름·복구
  요청 스키마를 고정했다. 운영 안정 기준은 정식 버전 승격 전까지 `1.0.0`이며,
  `BATON_CAL_SEASON_METADATA_ENABLED=false`와 보정 모드 `OFF`를 유지한다. CAL V7 배포 확인 뒤
  PRD-0006의 보정·복원 순서를 운영 환경에서 검증해야 한다. 실제 캘린더 앱의 이름 갱신도 남아 있다.

- 개인 구독과 진단 소비 코드는 `1.1.0-rc.2` 개발 소스를 대상으로 검증했지만 공식 계약 채택은 남아 있다.
  후보 소스·스키마 해시는 `contracts/baton-cal/candidate/source.properties`에 보관한다. 실제 릴리스
  자산·증명 검증을 마치기 전까지 구독은 기본 비활성을 유지한다. 팀 권한과 개인 구독을 함께 배포할 때는
  V34·V35 마이그레이션이 모두 필요하다. 공개 HTTPS와 실제 앱의 등록·변경·취소 반영은 아직 확인하지 않았다.
- 앱별 등록 안내의 외부 페이지 로그인·실제 구독 등록은 Google·Apple·Outlook 계정에서 확인해야 한다.
  브라우저 픽스처 검증은 실제 제공자 계정의 구독 성공을 뜻하지 않는다.

## 다음 실행 순서

1. 실제 공개 URL을 `BATON_HEALTH_URL`에 설정하고 `외부 상태 감시` 워크플로의 수동 실행을 먼저 통과시킨다. 이후 `BATON_EXTERNAL_MONITOR_ENABLED=true`로 예약 검사를 켜고 첫 예약 실행과 담당 계정의 GitHub Actions 실패 알림 수신을 확인한다.
2. 실제 파일럿 데이터를 넣기 전에 암호화 원격 저장소의 덤프와 보조 파일을 별도 환경에 복구한다. `last-restore-recovery-targets.tsv`에 따라 팀별 새 키 발급, 이전 링크의 `403` 응답과 복구 완료 뒤 재백업까지 실제 자격 증명으로 확인한다.
3. CAL 전용 Bearer를 소유자 전용 파일에 저장하고 `BATON_CAL_BEARER_TOKEN_FILE`에 절대 경로를 설정해 프로덕션 사전점검을 통과시킨다. 기존 문자열의 NFC·제어 문자 적합성을 점검한 뒤 PRD-0006 순서대로 CAL 캡처와 보정을 먼저 활성화한다. `./ops/check-integration-delivery.sh`, `./ops/show-integration-metrics.sh`와 DB 상태가 정상이면 전달을 켜고 실제 시즌 피드를 확인한다. 복원 훈련에서는 같은 복구 ID로 준비·전달 두 단계를 실행하고 CAL의 전체 완료 로그 뒤에만 복구 모드를 해제한다.
4. BRIEF 전용 Bearer 파일과 HTTPS origin을 준비하고 전달을 끈 상태에서 재조정 주기를 설정해 초기 신호와 아웃박스를 확인한다. 이후 전달을 켜 `./ops/check-integration-delivery.sh`와 `integration="brief"` 공통 지표가 정상인지 확인하고, 재시도·동일 이벤트 재전달·심각도 변경·해소가 실제 BRIEF 점검 항목에 반영되는지 확인한 뒤 새 token과 직전 token의 중첩 교체를 검증한다.
   BRIEF 조회·생성 서비스 API의 별도 Bearer·truststore도 배포하고, 실제 로그인·팀 구성원 연결을 거쳐
   오늘 화면의 주간 요약 조회·생성·기존 결과 재사용과 종료 시즌 조회를 원격 HTTPS에서 확인한다.
5. 외부 전송용 WATCH 모니터 토큰과 이벤트 수신기 토큰을 서로 다르게 배포하고 로그에 인증값이 남지 않는지 확인한다. 실제 공개 HTTPS에서 최초 상태 변경 이벤트와 응답 유실 뒤 같은 `eventId` 재전송이 BATON 인박스 한 건으로 수렴하고 WATCH 전달 적체가 비는지 확인한 뒤에만 운영 수신을 활성화한다.
6. 실제 Google·Naver·SMTP 자격 증명과 공개 HTTPS 출처에서 세 로그인 흐름, 콜백 로그 비노출, 이메일 수신·검증과 세션 쿠키 속성을 확인한다. `integration="email"`의 조치 대상 실패가 `0`이고 `./ops/check-integration-delivery.sh`가 성공하는지 확인한 뒤 계정 인증 게이트를 활성화한다.
   비밀번호 재설정은 V30 배포 후 실제 SMTP 링크 수신·새 비밀번호 로그인·기존 두 브라우저 세션
   거부를 확인한 뒤 `BATON_AUTH_PASSWORD_RESET_ENABLED=true`로 별도 활성화한다. 가입을 닫아도
   재설정만 운영할 수 있다. 공유 접근 키와 발급된 ROUND 참여권은 재설정으로 폐기되지 않는다.
   `/account`에서는 잘못된 현재 비밀번호가 기존 세션과 비밀번호를 유지하는지, 올바른 변경과
   모든 기기 로그아웃이 현재 브라우저를 즉시 로그아웃하고 다른 브라우저 세션을 다음 요청에서
   거부하는지 확인한다. 두 작업 뒤에도 같은 기기의 공유 접근 키가 유지되는지 함께 확인한다.
7. 실제 릴리스 다이제스트와 외부 coturn을 공개 HTTPS 스테이징에 배포해 UDP·TCP·TLS 할당과 미디어 중계, 실제 OAuth 계정의 동일 `sub` 입장과 키 중첩 회전을 확인한다.
8. WATCH `3a04e7b` 이상과 BATON 상태 조회·재점검 API를 배포한 뒤 역할 상세의 정상·오래된 결과·WATCH 장애·재점검 429를 실제 HTTPS에서 확인한다. 상태 표시는 이벤트 인박스 적용 없이 별도 현재 조회를 사용한다. WATCH 독립 복원은 WATCH 저장소의 `docs/runbooks/baton-snapshot-recovery.md`에 따라 BATON 최신 아웃박스 내보내기·대조·같은 리비전 재전송을 수행하며, 더 높은 원격 리비전은 자동으로 바꾸지 않는다.
   조회 분산·중복 합류·재점검 대기와 새 결과 안내, 최근 실패 원인·연속 실패 횟수,
   바통 전달·충돌 복구 대기 중 상태 조회, 점검 제외 사유·동기화 대기와 역할 이동 후 재점검
   대기시간 유지도 포함한다. 내부 오류의 최근 시도로 5분 이상 지난 판정이 갱신되지 않는지,
   시도·판정 시각의 별도 표시와 실제 새 판정 이후의 접수 안내 변경도 확인한다. 오프라인의
   이전 정상 표시 차단, 탭 복귀·재연결 재조회와 상태 조회 버튼의 실패 복구도 포함한다.
   접수·새 결과 확인 안내가 역할 이동과 오프라인·조회 실패 뒤에도 유지되는지, 새 재점검·
   URL 변경 시 초기화되는지 확인한다. 실제 스크린리더에서 수동 조회의 진행·결과와
   상태 변경만 알리고 같은 상태의 자동 조회를 반복해서 읽지 않는지도 확인한다. 2026-09-05 지정된
   `watch-staging.b4ton.com`의 공개 스모크는 네트워크 제한 밖에서도 DNS 조회에 실패했다.
   BATON 스테이징 주소·전용 시험 자료·인증 설정 파일이 제공되지 않아 공개 통합 검증은 남아 있다.

## 계정 권한 운영 전환의 남은 검증

- 팀을 전환하기 전에 공개 HTTPS에서 운영자 복구 키로 첫 관리자를 지정하고, 두 계정·두 기기로 초대 수락·권한 회수·기존 공유 링크 거부·본인 명의 인수인계 확인을 검증한다. 기존 팀은 자동 전환하지 않는다.
- 계정 권한 팀의 백업은 자동 복원 완료를 지원하지 않는다. `restore.sh`는 DB를 불러온 뒤 계정 팀을 발견하면 완료 처리를 중단하고 권한·초대 데이터를 유지한다. 서버를 공개하기 전에 별도 격리 환경에서 스냅샷 이후 권한 회수·초대 취소 이력과 대조하는 절차를 검증한다.

## 추가 기능의 운영 확인

- 무료 공휴일 연동은 공공데이터포털 ‘한국천문연구원_특일 정보’ 활용 신청과 일반 인증키(Decoding)
  파일이 필요하다. README의 무료 공휴일 연동 설정 후 실제 HTTPS에서 공휴일 이름·월 전환과
  API 장애 표시를 확인한다. 현재는 공식 XML 계약의 대역 응답만 검증했으며 기본 비활성을 유지한다.

- V36~V39 배포 전에 기존 결정·자료·계정 데이터가 유지되는지 확인한다. 수정 이력은 배포 후 수정부터 남으며 이전 변경을 소급하지 않는다.
- 공개 HTTPS의 두 기기에서 내 팀 전환, 자료 재확인 날짜, 개인 알림 설정·읽음 유지와 여러 시즌 검색을 확인한다.
- 공개 HTTPS에서 오늘의 재확인 목록, 여러 팀·진행 중 시즌의 내 업무와 권한 회수 반영, 스터디·팀 운영 템플릿 선택을 확인한다. 결정·자료·인수인계 본문의 초안은 같은 브라우저 탭에서 새로고침해 복구하고 계정 전환 시 섞이지 않는지 확인한다.
- 테스트용 계정의 비활성화에서 마지막 관리자 거부, 모든 기존 세션 거부, 로컬·소셜 재로그인 차단과 CAL 개인 구독의 실제 외부 해지 완료를 확인한다. 공유 키와 조직 기록은 보존한다.

## 파일럿 관찰

- 첫 그룹 스터디에서 운영 점검이 놓칠 뻔한 책임이나 인수인계 공백을 한 번 이상 미리 발견하는지 확인하고 오탐과 행동 문구를 기록한다.
- 과거 결정의 결과·이유·관련 역할을 검색 화면에서 짧은 흐름으로 다시 찾을 수 있는지 확인하고, 놓친 검색어·필터와 V14 이전 시각 미상 안내의 이해도를 기록한다.
